<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!-- Gantt Controller Template -->
    <t t-name="dobtor_project.GanttController">
        <Layout display="display" className="'o_gantt_view'">
            <!-- Search slots -->
            <t t-set-slot="layout-actions">
                <SearchBar toggler="searchBarToggler"/>
            </t>
            <t t-set-slot="control-panel-navigation-additional">
                <t t-component="searchBarToggler.component" t-props="searchBarToggler.props"/>
            </t>
            <t t-set-slot="control-panel-additional-actions">
                <CogMenu/>
            </t>

            <!-- Separator between control panel and toolbar -->
            <div class="o_gantt_cp_separator"/>

            <!-- Toolbar (rendered in o_content area, below control panel) -->
            <div class="o_gantt_toolbar">

                <!-- Group 1: Navigation -->
                <button class="o_gantt_tb_btn" t-on-click="onTodayClick" title="跳至今天"
                        t-att-disabled="isPlanningMode">
                    <i class="fa fa-crosshairs"/>
                </button>
                <!-- 壓縮向左 -->
                <button class="o_gantt_tb_btn" t-on-click="() => this.onCompactLeftClick()" title="壓縮向左">
                    <i class="fa fa-angle-double-left"/>
                </button>
                <div class="o_gantt_tb_dropdown o_gantt_tb_scale_dropdown">
                    <button class="o_gantt_tb_btn"
                            t-on-click.stop="onScaleMenuToggle"
                            t-att-title="'比例尺: ' + currentScaleLabel">
                        <i class="fa fa-search-plus"/>
                    </button>
                    <div class="o_gantt_tb_dropdown_menu" t-if="state.showScaleMenu">
                        <t t-foreach="scales" t-as="s" t-key="s.value">
                            <div t-att-class="'o_gantt_tb_dropdown_item' + (state.scale === s.value ? ' active' : '')"
                                 t-on-click.stop="() => { this.onScaleChange(s.value); this.state.showScaleMenu = false; }">
                                <t t-esc="s.label"/>
                            </div>
                        </t>
                    </div>
                </div>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 2: View -->
                <button class="o_gantt_tb_btn" t-on-click="onExpandAll" title="全部展開">
                    <i class="fa fa-expand"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="onCollapseAll" title="全部收合">
                    <i class="fa fa-compress"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="onSortToggle"
                        t-att-title="'排序: ' + sortLabel">
                    <i class="fa fa-sort-amount-asc"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onWeekTypeToggle()"
                        t-att-title="'週開始: ' + weekTypeLabel"
                        t-att-disabled="isPlanningMode">
                    <i class="fa fa-calendar"/>
                </button>
                <button t-att-class="'o_gantt_tb_btn' + (state.showListDetail ? ' active' : '')"
                        t-on-click="() => this.onListDetailToggle()" title="清單明細">
                    <i class="fa fa-list-ul"/>
                </button>
                <button t-att-class="'o_gantt_tb_btn' + (state.showIntersection ? ' active' : '')"
                        t-on-click="() => this.onIntersectionToggle()" title="交叉檢視">
                    <i class="fa fa-align-left"/>
                </button>
                <button t-att-class="'o_gantt_tb_btn' + (state.hideNonWorkingDays ? ' active' : '')"
                        t-on-click="() => this.onHideNonWorkingDaysToggle()" title="隱藏非工作日"
                        t-att-disabled="isPlanningMode">
                    <i class="fa fa-calendar-times-o"/>
                </button>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 3: Schedule -->
                <button class="o_gantt_tb_btn o_gantt_tb_btn_accent"
                        t-on-click="onSchedulerClick"
                        t-att-disabled="isLoading" title="自動排程">
                    <i t-att-class="'fa ' + (isLoading ? 'fa-spinner fa-spin' : 'fa-magic')"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onCatchUpClick()" title="追趕進度"
                        t-att-disabled="isPlanningMode">
                    <i class="fa fa-step-forward"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onRescheduleClick()" title="重新排程未完成"
                        t-att-disabled="isPlanningMode">
                    <i class="fa fa-forward"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onLevelResourcesClick()" title="資源平準化">
                    <i class="fa fa-balance-scale"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onAlignConstraintsClick()" title="對齊前置限制"
                        t-att-disabled="isLoading">
                    <i class="fa fa-indent"/>
                </button>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 4: Data -->
                <button class="o_gantt_tb_btn" t-on-click="onRefresh" title="重新整理">
                    <i class="fa fa-refresh"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onExportClick()" title="匯出 XML">
                    <i class="fa fa-download"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onImportClick()" title="匯入 XML">
                    <i class="fa fa-upload"/>
                </button>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 5: Output -->
                <button class="o_gantt_tb_btn" t-on-click="() => this.onPrintClick()" title="列印">
                    <i class="fa fa-print"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="onReportClick" title="PDF 報表">
                    <i class="fa fa-file-pdf-o"/>
                </button>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 6: Baseline -->
                <button class="o_gantt_tb_btn" t-on-click="() => this.onSaveBaselineClick()" title="儲存基線">
                    <i class="fa fa-camera"/>
                </button>
                <div class="o_gantt_tb_dropdown o_gantt_tb_baseline_dropdown">
                    <button t-att-class="'o_gantt_tb_btn' + (state.selectedBaselineId ? ' active' : '')"
                            t-on-click.stop="onBaselineMenuToggle" title="選擇基線">
                        <i class="fa fa-history"/>
                    </button>
                    <div class="o_gantt_tb_dropdown_menu" t-if="state.showBaselineMenu">
                        <div class="o_gantt_tb_dropdown_item" t-if="state.selectedBaselineId"
                             t-on-click.stop="() => { this.onBaselineClear(); this.state.showBaselineMenu = false; }">
                            <i class="fa fa-eye-slash"/> 清除基線
                        </div>
                        <t t-foreach="state.baselines" t-as="bl" t-key="bl.id">
                            <div t-att-class="'o_gantt_tb_dropdown_item' + (state.selectedBaselineId === bl.id ? ' active' : '')"
                                 t-on-click.stop="() => { this.onBaselineSelect(bl.id); this.state.showBaselineMenu = false; }">
                                <t t-esc="bl.name"/>
                            </div>
                        </t>
                        <div class="o_gantt_tb_dropdown_hint" t-if="!state.baselines.length">
                            尚無基線
                        </div>
                    </div>
                </div>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 7: Filter -->
                <button t-att-class="'o_gantt_tb_btn' + (state.filterCriticalPath ? ' active' : '')"
                        t-on-click="() => this.onFilterToggle('filterCriticalPath')" title="關鍵路徑">
                    <i class="fa fa-fire"/>
                </button>
                <button t-att-class="'o_gantt_tb_btn' + (state.filterOverdue ? ' active' : '')"
                        t-on-click="() => this.onFilterToggle('filterOverdue')" title="逾期"
                        t-att-disabled="isPlanningMode">
                    <i class="fa fa-hourglass-end"/>
                </button>
                <button t-att-class="'o_gantt_tb_btn' + (state.filterUnlinked ? ' active' : '')"
                        t-on-click="() => this.onFilterToggle('filterUnlinked')" title="未關聯">
                    <i class="fa fa-unlink"/>
                </button>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 8: Batch -->
                <button class="o_gantt_tb_btn" t-on-click="() => this.onBatchSetAuto()" title="選取任務設為自動">
                    <i class="fa fa-bolt"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onBatchSetManual()" title="選取任務設為手動">
                    <i class="fa fa-hand-paper-o"/>
                </button>
                <button class="o_gantt_tb_btn" t-on-click="() => this.onBatchRemoveConstraints()" title="移除選取任務限制">
                    <i class="fa fa-unlock"/>
                </button>

                <div class="o_gantt_tb_sep"/>

                <!-- Group 9: Misc -->
                <button class="o_gantt_tb_btn" t-on-click="() => this.onDetailPlanClick()" title="細節計畫">
                    <i class="fa fa-tasks"/>
                </button>
                <button t-att-class="'o_gantt_tb_btn' + (state.showViolationPanel ? ' active' : '')"
                        t-on-click="onViolationToggle" title="違規">
                    <i class="fa fa-exclamation-triangle"/>
                    <span class="o_gantt_tb_badge" t-if="state.violationCount > 0"
                          t-esc="state.violationCount"/>
                </button>

                <!-- Spacer pushes pager to right -->
                <div class="o_gantt_tb_spacer"/>

                <!-- Pager -->
                <span class="o_gantt_tb_pager" t-if="pagerText"
                      t-on-click="onPagerClick" title="點擊調整紀錄上限"
                      t-esc="pagerText"/>
            </div>

            <!-- Chart content -->
            <div class="o_gantt_content_wrapper">
                <t t-component="props.Renderer" t-props="rendererProps"/>
            </div>
        </Layout>
    </t>

    <!-- Gantt Controller Buttons Template -->
    <t t-name="dobtor_project.GanttController.Buttons"/>

    <!-- Gantt Renderer Template -->
    <t t-name="dobtor_project.GanttRenderer">
        <div class="o_gantt_container">
            <!-- Left Panel: Task List -->
            <div class="o_gantt_list" t-att-style="'width: ' + state.gutterWidth + 'px;'">
                <div class="o_gantt_list_header">
                    <span class="o_gantt_viewmode_btn" t-on-click.stop="() => this.setViewMode('resource')"
                          title="切換檢視（資源 / 網路圖 / 日曆）">
                        <i class="fa fa-th-large"/>
                    </span>
                    <span class="o_gantt_list_header_cell">任務</span>
                    <t t-if="props.showListDetail">
                        <span class="o_gantt_list_header_date">開始</span>
                        <span class="o_gantt_list_header_date">結束</span>
                    </t>
                    <t t-foreach="activeOptionalColumns" t-as="col" t-key="col.key">
                        <span class="o_gantt_list_header_col" t-esc="col.label"/>
                    </t>
                    <span class="o_gantt_col_chooser">
                        <span class="o_gantt_col_chooser_btn" t-on-click.stop="toggleColMenu"
                              title="選擇欄位">
                            <i class="fa fa-columns"/>
                        </span>
                        <div class="o_gantt_col_chooser_menu" t-if="state.showColMenu">
                            <t t-foreach="OPTIONAL_COLUMNS" t-as="col" t-key="col.key">
                                <div class="o_gantt_col_chooser_item"
                                     t-on-click.stop="() => this.toggleOptionalColumn(col.key)">
                                    <input type="checkbox" t-att-checked="isColumnActive(col.key)"
                                           style="pointer-events:none;"/>
                                    <span t-esc="col.label"/>
                                </div>
                            </t>
                        </div>
                    </span>
                </div>
                <div class="o_gantt_list_rows" t-ref="listRows">
                  <div t-att-style="_shouldUseVirtualScroll ? (_virtualScrollTotalHeightStyle + _virtualScrollOffsetStyle) : ''">
                    <t t-foreach="_shouldUseVirtualScroll ? visibleRows : flattenedRows" t-as="row" t-key="row._rowKey">
                        <div t-att-class="getRowClass(row)"
                             t-att-data-record-id="row._isGroup ? undefined : row.id"
                             t-att-data-row-id="row.id"
                             t-on-click="(ev) => this.onRowClick(row, ev)"
                             t-on-dblclick="(ev) => !row._isGroup and this.onTaskDblClick(row, ev)"
                             t-on-mouseenter="() => this.onRowHover(row.id)"
                             t-on-mouseleave="onRowLeave">
                            <t t-if="row._isGroup">
                                <span class="o_gantt_group_toggle"
                                      t-on-click.stop="() => this.onGroupToggle(row)">
                                    <i t-att-class="'fa ' + (row.fold ? 'fa-chevron-right' : 'fa-chevron-down')"/>
                                </span>
                                <t t-set="groupAvatarUrl" t-value="getGroupAvatarUrl(row)"/>
                                <img t-if="groupAvatarUrl"
                                     t-att-src="groupAvatarUrl"
                                     class="o_gantt_user_avatar o_gantt_group_avatar"
                                     loading="lazy"/>
                                <span class="o_gantt_group_name" t-esc="row.name"/>
                                <span class="o_gantt_group_count" t-esc="row.records?.length || 0"/>
                                <!-- Project progress indicator -->
                                <span class="o_gantt_group_progress_bar" t-if="getGroupProgress(row) > 0">
                                    <span class="o_gantt_group_progress_fill" t-att-style="getGroupProgressStyle(row)"/>
                                </span>
                                <t t-set="groupDateRange" t-value="getGroupDateRange(row)"/>
                                <span class="o_gantt_group_date_range"
                                      t-on-click.stop="(ev) => this.onGroupDateRangeClick(row, ev)"
                                      t-att-title="groupDateRange ? '點擊修改專案日期' : '點擊設定專案日期'">
                                    <i class="fa fa-calendar-o"/>
                                    <t t-if="groupDateRange">
                                        <t t-esc="groupDateRange"/>
                                    </t>
                                    <t t-else="">
                                        <span class="o_gantt_group_date_placeholder">設定日期</span>
                                    </t>
                                </span>
                                <span class="o_gantt_planning_badge" t-if="this.isGroupPlanningMode(row)">計劃模式</span>
                                <span class="o_gantt_clear_schedule_btn" t-if="!this.isGroupPlanningMode(row)"
                                      t-on-click.stop="(ev) => this.onClearScheduleClick(row, ev)"
                                      title="清除日期（回到計劃模式）">
                                    <i class="fa fa-calendar-times-o"/>
                                </span>
                                <span class="o_gantt_add_task_btn"
                                      t-on-click.stop="() => this.onAddTaskToGroup(row)"
                                      title="新增任務">
                                    <i class="fa fa-plus"/>
                                </span>
                                <span class="o_gantt_add_milestone_btn"
                                      t-on-click.stop="() => this.onAddMilestoneToGroup(row)"
                                      title="新增里程碑">
                                    <i class="fa fa-flag-checkered"/>
                                </span>
                            </t>
                            <t t-elif="row._isMilestoneRecord">
                                <!-- Milestone list row (aligned with task rows) -->
                                <span class="o_gantt_tree_drag_handle" title="拖曳排序">
                                    <i class="fa fa-ellipsis-v"/>
                                </span>
                                <!-- Alignment spacers matching task row structure -->
                                <span class="o_gantt_indent_spacer" t-att-style="getIndentStyle(row)"/>
                                <span class="o_gantt_task_fold_spacer"/>
                                <span class="o_gantt_milestone_number" t-if="row._milestoneNumber"
                                      t-esc="row._milestoneNumber"/>
                                <span class="o_gantt_task_name o_gantt_milestone_name">
                                    <t t-esc="row.display_name || row.name"/>
                                    <i t-if="row.is_reached"
                                       class="fa fa-check-circle text-success ms-1"
                                       title="已達成"/>
                                </span>
                                <span class="o_gantt_milestone_deadline" t-if="row.deadline"
                                      t-esc="row.deadline"/>
                                <span class="o_gantt_focus_btn"
                                      t-on-click.stop="() => this.onFocusClick(row)"
                                      title="滾動到圖型">
                                    <i class="fa fa-crosshairs"/>
                                </span>
                                <span class="o_gantt_delete_btn"
                                      t-on-click.stop="(ev) => this.onDeleteMilestoneClick(row)"
                                      title="刪除里程碑">
                                    <i class="fa fa-trash-o"/>
                                </span>
                            </t>
                            <t t-else="">
                                <!-- Tree drag handle -->
                                <span class="o_gantt_tree_drag_handle" title="拖曳排序">
                                    <i class="fa fa-ellipsis-v"/>
                                </span>
                                <!-- Indent spacer for tree hierarchy -->
                                <span class="o_gantt_indent_spacer" t-att-style="getIndentStyle(row)"/>
                                <!-- Fold chevron for parent tasks -->
                                <t t-if="row._hasChildren">
                                    <span class="o_gantt_task_fold_toggle"
                                          t-on-click.stop="() => this.onTaskFoldClick(row)">
                                        <i t-att-class="'fa ' + (this.isLoadingChildren(row) ? 'fa-spinner fa-spin' : row._isFolded ? 'fa-chevron-right' : 'fa-chevron-down')"/>
                                    </span>
                                </t>
                                <t t-else="">
                                    <span class="o_gantt_task_fold_spacer"/>
                                </t>
                                <!-- WBS number -->
                                <span class="o_gantt_wbs_number" t-if="row._wbsNumber" t-esc="row._wbsNumber"/>
                                <!-- Activity button (before avatar, always visible) -->
                                <span class="o_gantt_activity_btn"
                                      t-on-click.stop="(ev) => this.onActivityClick(row, ev)"
                                      title="活動">
                                    <i t-att-class="'fa fa-fw ' + getActivityButtonClass(row)"/>
                                </span>
                                <t t-set="userAvatarUrl" t-value="getUserAvatarUrl(row)"/>
                                <img t-if="userAvatarUrl"
                                     t-att-src="userAvatarUrl"
                                     class="o_gantt_user_avatar"
                                     loading="lazy"/>
                                <t t-set="extraUserCount" t-value="getExtraUserCount(row)"/>
                                <span class="o_gantt_extra_users" t-if="extraUserCount > 0">+<t t-esc="extraUserCount"/></span>
                                <!-- Task state indicator with dropdown -->
                                <t t-if="!row._isGroup">
                                    <span class="o_gantt_task_state"
                                          t-att-class="getTaskStateClass(row)"
                                          t-att-title="getTaskStateLabel(row)"
                                          t-on-click.stop="(ev) => this.onTaskStateClick(row, ev)">
                                        <i t-att-class="getTaskStateIcon(row)"/>
                                    </span>
                                    <div t-if="state.stateMenuRecordId === row.id"
                                         class="o_gantt_state_menu">
                                        <t t-foreach="getStateMenuItems(row)" t-as="si" t-key="si.value">
                                            <div t-att-class="'o_gantt_state_menu_item' + (si.active ? ' active' : '') + (si.disabled ? ' disabled' : '')"
                                                 t-on-click.stop="() => this.onStateMenuSelect(row, si)">
                                                <i t-att-class="si.icon"/>
                                                <span t-esc="si.label"/>
                                            </div>
                                        </t>
                                    </div>
                                </t>
                                <span class="o_gantt_task_name">
                                    <t t-esc="row.display_name || row[props.archInfo?.name]"/>
                                    <!-- Loop detection icon -->
                                    <t t-if="hasLoop(row)">
                                        <i class="o_gantt_loop_icon fa fa-refresh text-danger"
                                           title="偵測到相依迴圈"/>
                                    </t>
                                    <!-- Document count badge -->
                                    <t t-set="docCount" t-value="getDocCount(row)"/>
                                    <t t-if="docCount > 0">
                                        <span class="o_gantt_doc_badge" t-esc="docCount"/>
                                    </t>
                                    <!-- Plan action warning icon -->
                                    <t t-if="hasPlanAction(row)">
                                        <i class="o_gantt_plan_action_icon fa fa-exclamation text-warning"
                                           title="需要重新排程"/>
                                    </t>
                                </span>
                                <!-- Indent / Outdent buttons (visible on hover, after task name) -->
                                <span class="o_gantt_indent_btns">
                                    <span t-att-class="'o_gantt_outdent_btn' + (this.canOutdent(row) ? '' : ' disabled')"
                                          t-on-click.stop="() => this.onOutdentClick(row)" title="取消縮排">
                                        <i class="fa fa-arrow-left"/>
                                    </span>
                                    <span t-att-class="'o_gantt_indent_btn' + (this.canIndent(row) ? '' : ' disabled')"
                                          t-on-click.stop="() => this.onIndentClick(row)" title="縮排">
                                        <i class="fa fa-arrow-right"/>
                                    </span>
                                </span>
                                <t t-if="props.showListDetail">
                                    <span class="o_gantt_task_date" t-esc="getInfoStartDate(row)"/>
                                    <span class="o_gantt_task_date" t-esc="getInfoEndDate(row)"/>
                                </t>
                                <t t-foreach="activeOptionalColumns" t-as="col" t-key="col.key">
                                    <span class="o_gantt_task_col" t-esc="getOptionalColumnValue(row, col.key)"/>
                                </t>
                                <t t-set="resourceLevel" t-value="getResourceLevel(row)"/>
                                <span class="o_gantt_resource_level" t-if="resourceLevel"
                                      t-esc="resourceLevel"/>
                                <!-- Focus/scroll to bar -->
                                <span class="o_gantt_focus_btn"
                                      t-on-click.stop="() => this.onFocusClick(row)"
                                      title="滾動到圖型">
                                    <i class="fa fa-crosshairs"/>
                                </span>
                                <!-- Add subtask button (visible on hover) -->
                                <span class="o_gantt_add_subtask_btn"
                                      t-on-click.stop="() => this.onAddSubtask(row)"
                                      title="新增子任務">
                                    <i class="fa fa-plus"/>
                                </span>
                                <!-- Delete task button (visible on hover) -->
                                <span class="o_gantt_delete_btn"
                                      t-on-click.stop="(ev) => this.onDeleteTaskClick(row)"
                                      title="刪除任務">
                                    <i class="fa fa-trash-o"/>
                                </span>
                            </t>
                        </div>
                    </t>
                  </div>
                    <t t-if="!flattenedRows || !flattenedRows.length">
                        <div class="o_gantt_empty_state">
                            <i class="o_gantt_empty_icon fa fa-tasks"/>
                            <div class="o_gantt_empty_title">找不到任務</div>
                            <div class="o_gantt_empty_description">
                                建立含日期的任務以在甘特圖上顯示
                            </div>
                            <button class="o_gantt_empty_create_btn" t-if="props.onAddTask"
                                    t-on-click="() => props.onAddTask({})">
                                <i class="fa fa-plus"/> 新增任務
                            </button>
                        </div>
                    </t>
                </div>
            </div>

            <!-- Gutter₁: Task List ↔ Duration -->
            <div class="o_gantt_gutter" t-ref="gutter">
                <div class="o_gantt_gutter_handle"/>
            </div>

            <!-- Duration Panel -->
            <div class="o_gantt_duration_panel" t-att-style="'width: ' + state.durationWidth + 'px;'">
                <div class="o_gantt_duration_header">
                    <span>工期</span>
                </div>
                <div class="o_gantt_duration_rows" t-ref="durationRows">
                  <div t-att-style="_shouldUseVirtualScroll ? (_virtualScrollTotalHeightStyle + _virtualScrollOffsetStyle) : ''">
                    <t t-foreach="_shouldUseVirtualScroll ? visibleRows : flattenedRows" t-as="row" t-key="row._rowKey">
                        <div t-att-class="'o_gantt_duration_row' + (row._isGroup ? ' o_gantt_group_row' : '') + (state.selectedRowId === row.id ? ' o_gantt_row_selected' : '')"
                             t-att-data-row-id="row.id"
                             t-on-mouseenter="() => this.onRowHover(row.id)"
                             t-on-mouseleave="onRowLeave">
                            <t t-if="!row._isGroup">
                                <span class="o_gantt_duration_cell"
                                      t-att-title="getInfoDateRange(row)"
                                      t-on-click.stop="(ev) => this.onDurationClick(row, ev)">
                                    <t t-esc="getInfoDuration(row)"/>
                                </span>
                            </t>
                        </div>
                    </t>
                  </div>
                </div>
            </div>

            <!-- Gutter₂: Duration ↔ Timeline -->
            <div class="o_gantt_gutter o_gantt_gutter_duration" t-ref="gutterDuration">
                <div class="o_gantt_gutter_handle"/>
            </div>

            <!-- Right Panel: Timeline -->
            <div class="o_gantt_timeline" t-ref="timeline">
                <!-- Timeline Header -->
                <div class="o_gantt_timeline_header" t-att-style="'min-width: ' + timelineWidth + 'px;'">
                    <!-- Month Row -->
                    <div class="o_gantt_timeline_header_row o_gantt_timeline_header_months">
                        <t t-foreach="groupedMonths" t-as="month" t-key="month_index">
                            <div class="o_gantt_timeline_header_cell"
                                 t-att-style="'flex: 0 0 ' + (month.span * cellWidth) + 'px; width: ' + (month.span * cellWidth) + 'px;'">
                                <t t-esc="month.label"/>
                            </div>
                        </t>
                    </div>
                    <!-- Day/Week Row (with column hints) -->
                    <div class="o_gantt_timeline_header_row o_gantt_timeline_header_days">
                        <t t-foreach="timelineColumns" t-as="column" t-key="column_index">
                            <div t-att-class="getCellClass(column)"
                                 t-att-style="'flex: 0 0 ' + cellWidth + 'px; width: ' + cellWidth + 'px;'"
                                 t-att-title="getColumnHint(column)">
                                <span class="o_gantt_day_weekday" t-esc="column.weekday"/>
                                <span class="o_gantt_day_number" t-esc="column.label"/>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- Timeline Data -->
                <div class="o_gantt_timeline_data" t-att-style="'width: ' + timelineWidth + 'px;'" t-ref="timelineData"
                     t-on-dblclick="onTimelineDataDblClick">
                    <!-- Intersection Grid (vertical column lines) -->
                    <div class="o_gantt_grid_columns"
                         t-att-style="'height: ' + (flattenedRows.length * 44) + 'px;'">
                        <t t-foreach="timelineColumns" t-as="column" t-key="column_index">
                            <div t-att-class="'o_gantt_grid_col' + (column.isWeekend ? ' o_gantt_weekend' : '') + (column.isNonWorking ? ' o_gantt_nonworking' : '') + (column.isToday ? ' o_gantt_grid_today' : '')"
                                 t-att-style="'flex: 0 0 ' + cellWidth + 'px; width: ' + cellWidth + 'px;'"/>
                        </t>
                    </div>

                    <!-- Today Marker -->
                    <t t-if="todayPosition !== null">
                        <div class="o_gantt_today_marker"
                             t-att-style="'left: ' + todayPosition + 'px; height: ' + (flattenedRows.length * 44) + 'px;'"/>
                    </t>

                    <!-- Predecessor Arrows -->
                    <GanttArrows t-props="arrowProps"/>

                    <!-- Tooltip -->
                    <GanttTooltip archInfo="props.archInfo"
                                  getRecord="getModelRecord"
                                  getPredecessorCount="(id) => this.getPredecessorCount(id)"
                                  isPlanningMode="isPlanningMode"
                                  hoursPerDay="this.props.model.data?.calendarInfo?.hours_per_day"/>

                    <!-- Context Menu -->
                    <GanttContextMenu archInfo="props.archInfo"
                                      getRecord="getModelRecord"
                                      onAction="(rid, action) => this.onContextAction(rid, action)"/>

                    <!-- Timeline Rows. Virtualized the same way as the left list:
                         a padding-top/bottom spacer keeps each visible row at its
                         true Y, while arrows (separate SVG) still position from the
                         full flattenedRows indices, so they stay correct. -->
                    <div t-att-style="_shouldUseVirtualScroll ? _virtualScrollOffsetStyle : ''">
                    <t t-foreach="_shouldUseVirtualScroll ? visibleRows : flattenedRows" t-as="row" t-key="row._rowKey">
                        <div t-att-class="getTimelineRowClass(row)"
                             t-att-data-row-id="row.id"
                             t-on-click="(ev) => this.onRowClick(row, ev)"
                             t-on-mouseenter="() => this.onRowHover(row.id)"
                             t-on-mouseleave="onRowLeave">
                            <!-- Project aggregate bar for group rows -->
                            <t t-if="row._isGroup">
                                <div t-att-class="getGroupBarClass(row)"
                                     t-att-style="getGroupBarStyle(row)"
                                     t-att-title="row.name + ' (' + getGroupProgress(row) + '%)'"
                                     t-on-click="(ev) => this.onGroupBarClick(row, ev)">
                                    <div class="o_gantt_group_bar_progress"
                                         t-att-style="getGroupProgressStyle(row)"/>
                                </div>
                                <!-- Intersection bars (child tasks on group row) -->
                                <t t-if="props.showIntersection">
                                    <t t-foreach="getIntersectionBars(row)" t-as="iBar" t-key="iBar.id">
                                        <div class="o_gantt_intersection_bar"
                                             t-att-style="'left:' + iBar.left + 'px; width:' + iBar.width + 'px; top:' + iBar.top + 'px; background:' + iBar.color + ';'"
                                             t-att-title="iBar.name">
                                            <span class="o_gantt_intersection_label" t-esc="iBar.name"/>
                                        </div>
                                    </t>
                                </t>
                                <!-- Daily resource load bars on group row -->
                                <t t-foreach="getGroupLoadBars(row)" t-as="loadDay" t-key="loadDay.date">
                                    <div class="o_gantt_group_load_bar"
                                         t-att-style="'left:' + loadDay.left + 'px; width:' + loadDay.width + 'px;'"
                                         t-att-title="loadDay.label + ' 總工時'">
                                        <span class="o_gantt_group_load_label" t-esc="loadDay.label"/>
                                    </div>
                                </t>
                            </t>
                            <t t-if="!row._isGroup">
                                <t t-set="cachedBarStyle" t-value="getBarStyle(row)"/>
                                <div t-att-class="getBarClass(row)"
                                     t-att-style="cachedBarStyle + getBarCustomStyle(row)"
                                     t-att-data-record-id="row.id"
                                     t-on-click.stop="(ev) => this.onBarClick(row, ev)">
                                    <t t-if="!row._hasChildren">
                                        <!-- Progress bar fill + drag handle -->
                                        <div class="o_gantt_bar_progress"
                                             t-att-style="'width:' + Math.min(row._progress||0, 100) + '%;'"/>
                                        <div class="o_gantt_bar_progress_handle"
                                             t-att-style="'left:' + Math.min(row._progress||0, 100) + '%;'"/>
                                        <span class="o_gantt_bar_progress_text" t-if="row._progress > 0"
                                              t-esc="Math.round(row._progress) + '%'"/>
                                        <t t-set="barAvatarUrl" t-value="getUserAvatarUrl(row)"/>
                                        <img t-if="barAvatarUrl"
                                             t-att-src="barAvatarUrl"
                                             class="o_gantt_bar_avatar"
                                             loading="lazy"/>
                                        <span class="o_gantt_bar_label" t-if="row._showLabel" t-esc="row.display_name"/>
                                        <div class="o_gantt_bar_resize_handle o_gantt_bar_resize_left"/>
                                        <div class="o_gantt_bar_resize_handle o_gantt_bar_resize_right"/>
                                        <!-- Connector dots for arrow drawing -->
                                        <div class="o_gantt_connector o_gantt_connector_left" title="拖曳建立關聯 (開始)"/>
                                        <div class="o_gantt_connector o_gantt_connector_right" title="拖曳建立關聯 (結束)"/>
                                    </t>
                                    <t t-else="">
                                        <!-- Summary bracket bar with progress -->
                                        <div class="o_gantt_bar_progress"
                                             t-att-style="'width:' + Math.min(row._summaryProgress||0, 100) + '%;'"/>
                                        <span class="o_gantt_bar_progress_text" t-if="(row._summaryProgress||0) > 0"
                                              t-esc="Math.round(row._summaryProgress) + '%'"/>
                                        <span class="o_gantt_bar_label o_gantt_summary_label" t-if="row._showLabel" t-esc="row.display_name"/>
                                        <div class="o_gantt_summary_end"/>
                                        <!-- Connector dots for arrow drawing -->
                                        <div class="o_gantt_connector o_gantt_connector_left" title="拖曳建立關聯 (開始)"/>
                                        <div class="o_gantt_connector o_gantt_connector_right" title="拖曳建立關聯 (結束)"/>
                                    </t>
                                </div>
                                <!-- Constraint date marker (vertical line + label + connector) -->
                                <t t-if="hasConstraintDate(row)">
                                    <t t-set="cInfo" t-value="getConstraintInfo(row)"/>
                                    <div t-att-class="'o_gantt_constraint_marker' + (cInfo.hard ? ' o_gantt_constraint_hard' : '') + (cInfo.violated ? ' o_gantt_constraint_violated' : '') + (cInfo.isStartType ? ' o_gantt_constraint_start' : ' o_gantt_constraint_end')"
                                         t-att-style="cInfo.style"
                                         t-on-click.stop="(ev) => this.onConstraintClick(row, ev)"
                                         t-on-mouseenter="(ev) => this.onConstraintEnter(row, ev)"
                                         t-on-mouseleave="() => this.onConstraintLeave()">
                                        <span class="o_gantt_constraint_label" t-esc="cInfo.label"/>
                                        <div class="o_gantt_constraint_line"/>
                                        <!-- Horizontal connector to task bar -->
                                        <t t-if="cInfo.connector">
                                            <div t-att-class="'o_gantt_constraint_connector o_gantt_constraint_connector_' + cInfo.connector.direction"
                                                 t-att-style="'left:' + cInfo.connector.left + 'px; width:' + cInfo.connector.width + 'px;'"/>
                                        </t>
                                    </div>
                                    <!-- Constraint custom tooltip -->
                                    <div t-if="state.constraintTooltipId === row.id"
                                         class="o_gantt_constraint_tooltip"
                                         t-att-style="state.constraintTooltipStyle">
                                        <div class="o_gantt_constraint_tooltip_type">
                                            <i t-att-class="'fa fa-fw ' + (cInfo.hard ? 'fa-lock' : 'fa-unlock-alt')"/>
                                            <span t-esc="cInfo.typeLabel"/>
                                        </div>
                                        <div class="o_gantt_constraint_tooltip_date">
                                            <span t-esc="cInfo.dateStr"/>
                                        </div>
                                        <div t-if="cInfo.violated" class="o_gantt_constraint_tooltip_warn">
                                            <i class="fa fa-exclamation-triangle"/>
                                            <span>約束違反</span>
                                        </div>
                                        <div class="o_gantt_constraint_tooltip_current">
                                            <span t-esc="cInfo.currentLabel"/>
                                        </div>
                                    </div>
                                </t>
                                <!-- Deadline marker (diamond) - draggable -->
                                <t t-if="hasDeadline(row)">
                                    <div t-att-class="getDeadlineClass(row) + ' o_gantt_deadline_draggable'"
                                         t-att-style="getDeadlineStyle(row)"
                                         t-att-title="'截止日: ' + formatDeadline(row)"/>
                                </t>
                                <!-- Deadline lag bar (colored region between bar end and deadline) -->
                                <t t-set="lagInfo" t-value="getDeadlineLagInfo(row)"/>
                                <t t-if="lagInfo">
                                    <div t-att-class="'o_gantt_deadline_lag_bar' + (lagInfo.overdue ? ' o_gantt_deadline_lag_overdue' : '')"
                                         t-att-style="'left:' + lagInfo.left + 'px; width:' + lagInfo.width + 'px;'">
                                        <span class="o_gantt_deadline_lag_text" t-esc="lagInfo.label"/>
                                    </div>
                                </t>
                                <!-- Date done marker (checkmark) -->
                                <t t-if="row._dateDone">
                                    <div class="o_gantt_done_marker"
                                         t-att-style="getDoneMarkerStyle(row)"
                                         t-att-title="'完成: ' + row._dateDone.toFormat('yyyy/M/d')"/>
                                </t>
                                <!-- Task info overlay (ES/LS/EF/LF) -->
                                <t t-set="rowTaskInfos" t-value="getTaskInfos(row)"/>
                                <t t-if="rowTaskInfos.length > 0">
                                    <t t-foreach="rowTaskInfos" t-as="info" t-key="info_index">
                                        <div class="o_gantt_task_info_overlay"
                                             t-att-style="cachedBarStyle">
                                            <span class="o_gantt_info_lu" t-esc="info.leftUp"/>
                                            <span class="o_gantt_info_ru" t-esc="info.rightUp"/>
                                            <span class="o_gantt_info_ld" t-esc="info.leftDown"/>
                                            <span class="o_gantt_info_rd" t-esc="info.rightDown"/>
                                        </div>
                                    </t>
                                </t>
                                <!-- Ghost/Baseline bars -->
                                <t t-foreach="getGhostBars(row)" t-as="ghostBar" t-key="ghostBar.id">
                                    <div class="o_gantt_ghost_bar"
                                         t-att-style="getGhostBarStyle(ghostBar)"
                                         t-att-title="'基線: ' + (ghostBar.name || '')"/>
                                </t>
                                <!-- Resource conflict indicator (only while the
                                     intersection/resource overlay is enabled, so
                                     the resource index isn't built every render). -->
                                <t t-if="props.showIntersection and hasResourceConflict(row)">
                                    <div class="o_gantt_resource_conflict_indicator"
                                         t-att-style="cachedBarStyle"
                                         title="資源衝突：指派重疊"/>
                                </t>
                                <!-- Resource load bars (detail plan) -->
                                <t t-foreach="getLoadBars(row)" t-as="loadBar" t-key="loadBar.id">
                                    <div class="o_gantt_load_bar"
                                         t-att-style="getLoadBarStyle(loadBar)"
                                         t-att-title="loadBar.name"/>
                                </t>
                            </t>
                        </div>
                    </t>
                    </div>
                </div>
            </div>

            <!-- Violation Panel (slide-in from right) -->
            <div t-att-class="'o_gantt_violation_panel' + (props.showViolationPanel ? ' o_gantt_violation_panel_visible' : '')">
                <div class="o_gantt_violation_header">
                    <span class="o_gantt_violation_title">違規</span>
                    <span class="o_gantt_violation_close" t-on-click="props.onViolationClose">
                        <i class="fa fa-times"/>
                    </span>
                </div>
                <div class="o_gantt_violation_body">
                    <t t-if="!props.violations || props.violations.length === 0">
                        <div class="o_gantt_violation_empty">
                            <i class="fa fa-check-circle"/>
                            <span>未發現違規</span>
                        </div>
                    </t>
                    <t t-foreach="props.violations || []" t-as="v" t-key="v_index">
                        <div t-att-class="'o_gantt_violation_item o_gantt_violation_' + v.severity"
                             t-on-click="() => this.onViolationClick(v.task_id)">
                            <i t-att-class="'fa ' + (v.type === 'loop' ? 'fa-refresh' : v.type === 'overdue' ? 'fa-clock-o' : v.type === 'constraint' ? 'fa-lock' : v.type === 'resource_overload' ? 'fa-users' : 'fa-unlink')"/>
                            <div class="o_gantt_violation_content">
                                <div class="o_gantt_violation_task" t-esc="v.task_name"/>
                                <div class="o_gantt_violation_message" t-esc="v.message"/>
                            </div>
                            <span class="o_gantt_violation_severity_badge" t-esc="v.severity"/>
                        </div>
                    </t>
                </div>
            </div>

            <!-- Task Inspector Panel (slide-in from right) -->
            <GanttInspector
                visible="props.showInspectorPanel"
                record="inspectorRecord"
                archInfo="props.archInfo"
                predecessors="inspectorPredecessors"
                isPlanningMode="isPlanningMode"
                onClose="props.onInspectorClose"
                onFieldChange="(rid, field, val) => this.onInspectorFieldChange(rid, field, val)"
                onOpenForm="(record) => this.props.onRecordClick(record)"
                onDetailPlan="(recordId) => this.props.onRecordClick({ id: recordId, action: 'detail_plan' })"
                onDeletePredecessor="(predId) => this.onDeletePredecessor(predId)"
                onUpdatePredecessor="(predId, vals) => this.onUpdatePredecessor(predId, vals)"
                milestoneLinkedTasks="inspectorMilestoneLinks"
                onRemoveMilestoneLink="(taskId) => this.onRemoveMilestoneLink(taskId)"
                calendarInfo="props.model.data?.calendarInfo"
            />

            <!-- Mini-map scroll indicator -->
            <div class="o_gantt_scrollmap_container">
                <GanttScrollMap t-props="scrollMapProps"/>
            </div>

            <!-- Alternate views overlay (resource / network / calendar).
                 Rendered on top of the gantt; the gantt DOM is left intact. -->
            <GanttAltView t-if="state.viewMode !== 'gantt'"
                          mode="state.viewMode"
                          model="props.model"
                          archInfo="props.archInfo"
                          onSetMode="(m) => this.setViewMode(m)"
                          onClose="() => this.setViewMode('gantt')"/>
        </div>
    </t>

    <!-- Gantt ScrollMap Template -->
    <t t-name="dobtor_project.GanttScrollMap">
        <canvas t-ref="scrollMapCanvas"
                class="o_gantt_scrollmap_canvas"
                t-on-pointerdown="_onPointerDown"/>
    </t>

    <!-- Gantt Tooltip Template (Apple Popover Style) -->
    <t t-name="dobtor_project.GanttTooltip">
        <div t-ref="tooltip"
             t-att-class="'o_gantt_tooltip' + (state.visible ? ' o_gantt_tooltip_visible' : '')"
             t-att-style="'left: ' + state.x + 'px; top: ' + state.y + 'px;'">
            <t t-if="tooltipData">
                <!-- Header section: name + badges -->
                <div class="o_gantt_tooltip_header">
                    <div class="o_gantt_tooltip_title" t-esc="tooltipData.name"/>
                    <div class="o_gantt_tooltip_badges" t-if="tooltipData.isCritical || tooltipData.isMilestone">
                        <span class="o_gantt_tooltip_badge o_gantt_tooltip_badge_critical" t-if="tooltipData.isCritical">關鍵路徑</span>
                        <span class="o_gantt_tooltip_badge o_gantt_tooltip_badge_milestone" t-if="tooltipData.isMilestone">里程碑</span>
                    </div>
                </div>
                <!-- Schedule section -->
                <div class="o_gantt_tooltip_section">
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.dateStart">
                        <span class="o_gantt_tooltip_label">開始</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.dateStart"/>
                    </div>
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.dateEnd">
                        <span class="o_gantt_tooltip_label">結束</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.dateEnd"/>
                    </div>
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.duration">
                        <span class="o_gantt_tooltip_label">工期</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.duration"/>
                    </div>
                </div>
                <!-- Status section -->
                <div class="o_gantt_tooltip_section" t-if="tooltipData.progress || tooltipData.scheduleMode || tooltipData.fixedCalcType">
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.progress">
                        <span class="o_gantt_tooltip_label">進度</span>
                        <span class="o_gantt_tooltip_value">
                            <span class="o_gantt_tooltip_progress_bar">
                                <span class="o_gantt_tooltip_progress_fill" t-att-style="'width:' + tooltipData.progressPct + '%;'"/>
                            </span>
                            <t t-esc="tooltipData.progress"/>
                        </span>
                    </div>
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.scheduleMode">
                        <span class="o_gantt_tooltip_label">排程</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.scheduleMode"/>
                    </div>
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.fixedCalcType">
                        <span class="o_gantt_tooltip_label">計算</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.fixedCalcType"/>
                    </div>
                </div>
                <!-- Resource / Assignee section -->
                <div class="o_gantt_tooltip_section" t-if="tooltipData.resource || tooltipData.predecessorCount">
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.resource">
                        <span class="o_gantt_tooltip_label">負責人</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.resource"/>
                    </div>
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.predecessorCount">
                        <span class="o_gantt_tooltip_label">關聯</span>
                        <span class="o_gantt_tooltip_value">
                            <t t-esc="tooltipData.predecessorCount"/> 筆前置關係
                        </span>
                    </div>
                </div>
                <!-- Constraints section -->
                <div class="o_gantt_tooltip_section" t-if="tooltipData.deadline || tooltipData.constraint">
                    <div t-att-class="'o_gantt_tooltip_row' + (tooltipData.deadlineOverdue ? ' o_gantt_tooltip_row_danger' : '')" t-if="tooltipData.deadline">
                        <span class="o_gantt_tooltip_label">截止日</span>
                        <span class="o_gantt_tooltip_value">
                            <t t-esc="tooltipData.deadline"/>
                            <i class="fa fa-exclamation-triangle ms-1" t-if="tooltipData.deadlineOverdue"/>
                        </span>
                    </div>
                    <div class="o_gantt_tooltip_row" t-if="tooltipData.constraint">
                        <span class="o_gantt_tooltip_label">限制</span>
                        <span class="o_gantt_tooltip_value" t-esc="tooltipData.constraint"/>
                    </div>
                </div>
                <!-- Arrow pointing down to the bar -->
                <div class="o_gantt_tooltip_arrow"/>
            </t>
        </div>
    </t>

    <!-- Gantt Inspector Panel Template -->
    <t t-name="dobtor_project.GanttInspector">
        <div t-att-class="'o_gantt_inspector_panel' + (props.visible ? ' o_gantt_inspector_visible' : '')">
            <div class="o_gantt_inspector_header">
                <span class="o_gantt_inspector_title">檢查器</span>
                <span class="o_gantt_inspector_action" t-on-click="() => props.onOpenForm(props.record)" title="開啟表單"
                      t-if="props.record">
                    <i class="fa fa-external-link"/>
                </span>
                <span class="o_gantt_inspector_action" t-on-click="props.onClose">
                    <i class="fa fa-times"/>
                </span>
            </div>
            <div class="o_gantt_inspector_body" t-if="props.record">
                <!-- Milestone-specific inspector -->
                <t t-if="this.isMilestoneRecord">
                    <div class="o_gantt_inspector_section">
                        <div class="o_gantt_inspector_section_title" t-on-click="() => this.toggleSection('general')">
                            <i t-att-class="'fa fa-chevron-' + (state.expandedSections.general ? 'down' : 'right')"/>
                            <i class="fa fa-flag-checkered" style="color: var(--gantt-accent-orange); margin-right: 4px;"/>
                            里程碑
                        </div>
                        <div t-if="state.expandedSections.general">
                            <div class="o_gantt_inspector_field">
                                <label>名稱</label>
                                <input type="text" t-att-value="props.record.display_name || props.record.name"
                                       t-on-blur="(ev) => this.onFieldBlur('name', ev)"/>
                            </div>
                            <div class="o_gantt_inspector_field">
                                <label>期限</label>
                                <DateTimeInput type="'datetime'"
                                               value="props.record._dateStart || undefined"
                                               onApply="(dt) => this.onDateTimeApply('deadline_datetime', dt)"/>
                            </div>
                            <div class="o_gantt_inspector_field">
                                <label>已達成</label>
                                <input type="checkbox"
                                       t-att-checked="props.record.is_reached"
                                       t-on-change="(ev) => this.onMilestoneReachedChange(ev)"/>
                            </div>
                            <div class="o_gantt_inspector_field o_gantt_inspector_field_color">
                                <label>顏色</label>
                                <ul class="o_gantt_colorpicker">
                                    <t t-foreach="this.ganttColors" t-as="color" t-key="color_index">
                                        <li t-on-click.stop="() => this.onColorSelect(color_index)"
                                            t-att-class="color_index === this.currentColorIndex ? 'selected' : ''"
                                            t-att-title="this.ganttColorName(color_index)">
                                            <a t-attf-class="o_gantt_color_#{color_index}"/>
                                        </li>
                                    </t>
                                </ul>
                            </div>
                        </div>
                    </div>
                    <!-- Milestone linked tasks section -->
                    <div class="o_gantt_inspector_section">
                        <div class="o_gantt_inspector_section_title mt-2"
                             t-on-click="() => this.toggleSection('milestoneLinks')">
                            <i t-att-class="'fa fa-chevron-' + (state.expandedSections.milestoneLinks ? 'down' : 'right')"/>
                            關聯任務
                            <span class="badge bg-secondary ms-1"
                                  t-esc="(props.milestoneLinkedTasks || []).length"/>
                        </div>
                        <div t-if="state.expandedSections.milestoneLinks">
                            <div t-if="!(props.milestoneLinkedTasks || []).length"
                                 class="o_gantt_inspector_empty_hint">
                                尚無關聯任務
                            </div>
                            <div t-foreach="props.milestoneLinkedTasks || []" t-as="link" t-key="link.task_id"
                                 class="o_gantt_inspector_milestone_task">
                                <span class="o_gantt_inspector_milestone_task_wbs"
                                      t-if="link.task_wbs" t-esc="link.task_wbs"/>
                                <span class="o_gantt_inspector_milestone_task_name"
                                      t-esc="link.task_name"/>
                                <span class="o_gantt_inspector_milestone_task_delete"
                                      t-on-click.stop="() => props.onRemoveMilestoneLink(link.task_id)"
                                      title="移除關聯">
                                    <i class="fa fa-trash-o"/>
                                </span>
                            </div>
                        </div>
                    </div>
                </t>
                <!-- Task-specific inspector -->
                <t t-else="">
                <!-- 一般 Section -->
                <div class="o_gantt_inspector_section">
                    <div class="o_gantt_inspector_section_title" t-on-click="() => this.toggleSection('general')">
                        <i t-att-class="'fa fa-chevron-' + (state.expandedSections.general ? 'down' : 'right')"/>
                        一般
                    </div>
                    <div t-if="state.expandedSections.general">
                        <!-- 受指派人 -->
                        <div class="o_gantt_inspector_field o_gantt_inspector_field_users">
                            <label>受指派人</label>
                            <div class="o_gantt_user_picker">
                                <t t-foreach="this.userChips" t-as="chip" t-key="chip.id">
                                    <span class="o_gantt_user_chip">
                                        <img t-att-src="'/web/image/res.users/' + chip.id + '/avatar_128'"
                                             class="o_gantt_user_chip_avatar"/>
                                        <span t-esc="chip.name"/>
                                        <i class="fa fa-times o_gantt_user_chip_remove"
                                           t-on-click.stop="() => this.onUserRemove(chip.id)"/>
                                    </span>
                                </t>
                                <input type="text" class="o_gantt_user_search"
                                       placeholder="搜尋使用者..."
                                       t-att-value="state.userQuery"
                                       t-on-input="(ev) => this.onUserSearchInput(ev)"
                                       t-on-focus="() => this.onUserInputFocus()"
                                       t-on-blur="() => this.onUserInputBlur()"/>
                                <div class="o_gantt_user_dropdown"
                                     t-if="state.showUserDropdown and state.userResults.length">
                                    <t t-foreach="state.userResults" t-as="user" t-key="user[0]">
                                        <div class="o_gantt_user_dropdown_item"
                                             t-on-mousedown.prevent="() => this.onUserSelect(user[0], user[1])">
                                            <img t-att-src="'/web/image/res.users/' + user[0] + '/avatar_128'"
                                                 class="o_gantt_user_dropdown_avatar"/>
                                            <span t-esc="user[1]"/>
                                        </div>
                                    </t>
                                </div>
                            </div>
                        </div>
                        <div class="o_gantt_inspector_field">
                            <label>名稱</label>
                            <input type="text" t-att-value="props.record.display_name"
                                   t-on-blur="(ev) => this.onFieldBlur('name', ev)"/>
                        </div>
                        <div class="o_gantt_inspector_field">
                            <label>排程模式</label>
                            <select t-on-change="(ev) => this.onSelectChange(props.archInfo.scheduleMode || 'schedule_mode', ev)">
                                <option value="auto" t-att-selected="props.record._scheduleMode === 'auto'">自動</option>
                                <option value="manual" t-att-selected="props.record._scheduleMode === 'manual'">手動</option>
                            </select>
                        </div>
                        <div class="o_gantt_inspector_field">
                            <label>長條名稱</label>
                            <input type="checkbox"
                                   t-att-checked="props.record[props.archInfo.onGantt || 'on_gantt']"
                                   t-on-change="(ev) => this.onCheckboxChange(props.archInfo.onGantt || 'on_gantt', ev)"/>
                        </div>
                        <div class="o_gantt_inspector_field o_gantt_inspector_field_color">
                            <label>長條顏色</label>
                            <ul class="o_gantt_colorpicker">
                                <t t-foreach="this.ganttColors" t-as="color" t-key="color_index">
                                    <li t-on-click.stop="() => this.onColorSelect(color_index)"
                                        t-att-class="color_index === this.currentColorIndex ? 'selected' : ''"
                                        t-att-title="this.ganttColorName(color_index)">
                                        <a t-attf-class="o_gantt_color_#{color_index}"/>
                                    </li>
                                </t>
                            </ul>
                        </div>
                        <!-- 細節計畫 button -->
                        <div class="o_gantt_inspector_field" t-if="props.archInfo.loadBarModel">
                            <label>細節計畫</label>
                            <button class="o_gantt_inspector_btn" t-on-click="() => this.onDetailPlanClick()">
                                <i class="fa fa-tasks"/> 開啟
                            </button>
                        </div>
                    </div>
                </div>
                <!-- 排程 Section -->
                <div class="o_gantt_inspector_section">
                    <div class="o_gantt_inspector_section_title" t-on-click="() => this.toggleSection('schedule')">
                        <i t-att-class="'fa fa-chevron-' + (state.expandedSections.schedule ? 'down' : 'right')"/>
                        排程
                    </div>
                    <div t-if="state.expandedSections.schedule">
                        <!-- 開始/結束: hide in planning mode -->
                        <t t-if="!props.isPlanningMode">
                            <div class="o_gantt_inspector_field">
                                <label>開始</label>
                                <DateTimeInput type="'datetime'"
                                               value="props.record._dateStart || undefined"
                                               onApply="(dt) => this.onDateTimeApply(props.archInfo.dateStart || 'date_start', dt)"/>
                            </div>
                            <div class="o_gantt_inspector_field">
                                <label>結束</label>
                                <DateTimeInput type="'datetime'"
                                               value="props.record._dateEnd || undefined"
                                               onApply="(dt) => this.onDateTimeApply(props.archInfo.dateStop || 'date_end', dt)"/>
                            </div>
                        </t>
                        <div class="o_gantt_inspector_field" t-if="this.calendarActive and this.workingDurationLabel">
                            <label>工作工期</label>
                            <span t-esc="this.workingDurationLabel"/>
                        </div>
                        <div class="o_gantt_inspector_field">
                            <label>進度</label>
                            <t t-if="props.record._hasChildren">
                                <span class="o_gantt_inspector_progress_val"
                                      t-esc="Math.round(props.record._summaryProgress || 0) + '% (自動)'"/>
                            </t>
                            <t t-elif="props.record._progressMode === 'timesheet'">
                                <span class="o_gantt_inspector_progress_val"
                                      t-esc="Math.round(props.record._progress || 0) + '% (工時單)'"/>
                            </t>
                            <t t-else="">
                                <div class="o_gantt_inspector_progress_row">
                                    <input type="range" min="0" max="100"
                                           t-att-value="Math.round(props.record._progress || 0)"
                                           t-on-input="onProgressChange"/>
                                    <span class="o_gantt_inspector_progress_val"
                                          t-esc="Math.round(props.record._progress || 0) + '%'"/>
                                </div>
                            </t>
                        </div>
                        <!-- 計算方式 (fixed_calc_type) -->
                        <div class="o_gantt_inspector_field" t-if="this.fixedCalcTypeLabel">
                            <label>計算方式</label>
                            <select t-on-change="(ev) => this.onSelectChange(props.archInfo.fixedCalcType || 'fixed_calc_type', ev)">
                                <option value="duration" t-att-selected="props.record[props.archInfo.fixedCalcType || 'fixed_calc_type'] === 'duration'">固定工期</option>
                                <option value="work" t-att-selected="props.record[props.archInfo.fixedCalcType || 'fixed_calc_type'] === 'work'">固定工時</option>
                            </select>
                        </div>
                        <!-- 限制: hide in planning mode -->
                        <t t-if="!props.isPlanningMode">
                            <div class="o_gantt_inspector_field">
                                <label>限制類型</label>
                                <select t-on-change="(ev) => this.onSelectChange(props.archInfo.constrainType || 'constrain_type', ev)">
                                    <t t-foreach="[['asap','儘早開始'],['alap','儘晚開始'],['snet','不早於...開始'],['snlt','不晚於...開始'],['fnet','不早於...完成'],['fnlt','不晚於...完成'],['mso','必須在...開始'],['mfo','必須在...完成']]"
                                       t-as="ct" t-key="ct[0]">
                                        <option t-att-value="ct[0]"
                                                t-att-selected="props.record[props.archInfo.constrainType || 'constrain_type'] === ct[0]"
                                                t-esc="ct[1]"/>
                                    </t>
                                </select>
                            </div>
                            <div class="o_gantt_inspector_field"
                                 t-if="props.record[props.archInfo.constrainType || 'constrain_type'] and props.record[props.archInfo.constrainType || 'constrain_type'] !== 'asap' and props.record[props.archInfo.constrainType || 'constrain_type'] !== 'alap'">
                                <label>限制日期</label>
                                <DateTimeInput type="'datetime'"
                                               value="props.record._constrainDate || undefined"
                                               onApply="(dt) => this.onDateTimeApply(props.archInfo.constrainDate || 'constrain_date', dt)"/>
                            </div>
                        </t>
                    </div>
                </div>
                <!-- 相關性 Section -->
                <div class="o_gantt_inspector_section">
                    <div class="o_gantt_inspector_section_title" t-on-click="() => this.toggleSection('predecessors')">
                        <i t-att-class="'fa fa-chevron-' + (state.expandedSections.predecessors ? 'down' : 'right')"/>
                        相關性
                        <span class="o_gantt_inspector_section_count" t-if="props.predecessors and props.predecessors.length"
                              t-esc="props.predecessors.length"/>
                    </div>
                    <div t-if="state.expandedSections.predecessors">
                        <div class="o_gantt_inspector_pred_item" t-foreach="props.predecessors || []" t-as="pred" t-key="pred_index">
                            <span class="o_gantt_inspector_pred_name" t-esc="pred._linkedName || '...'"/>
                            <span class="o_gantt_inspector_link_type">
                                <span t-esc="pred._linkFrom"/>
                                <i class="fa fa-arrow-right"/>
                                <span t-esc="pred._linkTo"/>
                            </span>
                            <div class="o_gantt_inspector_pred_lag_edit">
                                <span class="o_gantt_inspector_lag_label">延遲</span>
                                <t t-if="this.isEditingLag(pred)">
                                    <input type="text" class="o_gantt_inspector_lag_input"
                                           placeholder="1d3h30m"
                                           t-att-value="this.lagInputValue(pred)"
                                           t-on-blur="(ev) => this.onLagInputBlur(pred, ev)"
                                           t-on-keydown="(ev) => this.onLagInputKeydown(pred, ev)"
                                           t-ref="'lagInput_' + pred._predIdentifier"/>
                                </t>
                                <t t-else="">
                                    <span class="o_gantt_inspector_lag_display"
                                          t-on-click.stop="() => this.onLagDisplayClick(pred)"
                                          t-esc="this.lagDisplayLabel(pred)"/>
                                </t>
                            </div>
                            <span class="o_gantt_inspector_pred_blocking"
                                  t-att-title="pred.enable_blocking ? '阻擋中（前置未完成時等待）' : '不阻擋'"
                                  t-att-class="'text-' + (pred.enable_blocking ? 'warning' : 'muted')">
                                <i t-att-class="'fa ' + (pred.enable_blocking ? 'fa-lock' : 'fa-unlock')"/>
                            </span>
                            <span class="o_gantt_inspector_pred_delete"
                                  t-if="props.onDeletePredecessor"
                                  t-on-click.stop="() => props.onDeletePredecessor(pred._predIdentifier)"
                                  title="移除關聯">
                                <i class="fa fa-chain-broken"/>
                            </span>
                        </div>
                        <div class="o_gantt_inspector_list_empty" t-if="!props.predecessors || props.predecessors.length === 0">
                            無相關性
                        </div>
                    </div>
                </div>
                </t><!-- /t-else task inspector -->
            </div>
            <div class="o_gantt_inspector_empty" t-if="!props.record">
                <i class="fa fa-mouse-pointer"/>
                <span>選擇任務以檢視</span>
            </div>
        </div>
    </t>

    <!-- Gantt Context Menu Template -->
    <t t-name="dobtor_project.GanttContextMenu">
        <div t-ref="contextMenu"
             t-att-class="'o_gantt_context_menu' + (state.visible ? ' o_gantt_context_menu_visible' : '')"
             t-att-style="'left: ' + state.x + 'px; top: ' + state.y + 'px;'">
            <t t-foreach="menuItems" t-as="item" t-key="item.id">
                <div class="o_gantt_context_menu_item"
                     t-on-click.stop="() => this.onMenuItemClick(item.id)">
                    <i t-att-class="'fa ' + item.icon"/>
                    <span t-esc="item.label"/>
                </div>
            </t>
        </div>
    </t>

    <!-- Gantt Arrows SVG Template -->
    <t t-name="dobtor_project.GanttArrows">
        <svg class="o_gantt_arrow_container" t-att-style="'width: 100%; height: 100%;'">
            <defs>
                <!-- Normal arrowhead marker (chevron >) -->
                <marker id="gantt_arrowhead" markerWidth="8" markerHeight="8"
                        refX="6" refY="4" orient="auto" markerUnits="userSpaceOnUse">
                    <path d="M1,1 L6,4 L1,7" class="o_gantt_arrow_marker"/>
                </marker>
                <!-- Highlight arrowhead marker -->
                <marker id="gantt_arrowhead_highlight" markerWidth="8" markerHeight="8"
                        refX="6" refY="4" orient="auto" markerUnits="userSpaceOnUse">
                    <path d="M1,1 L6,4 L1,7" class="o_gantt_arrow_marker o_gantt_arrow_marker_highlight"/>
                </marker>
                <!-- Critical arrowhead marker -->
                <marker id="gantt_arrowhead_critical" markerWidth="8" markerHeight="8"
                        refX="6" refY="4" orient="auto" markerUnits="userSpaceOnUse">
                    <path d="M1,1 L6,4 L1,7" class="o_gantt_arrow_marker o_gantt_arrow_marker_critical"/>
                </marker>
            </defs>
            <t t-foreach="arrowPaths" t-as="arrow" t-key="arrow.id">
                <!-- Wider invisible hit area for easier clicking -->
                <path t-att-d="arrow.d"
                      class="o_gantt_arrow_hitarea"
                      t-att-data-pred-id="arrow.id"
                      fill="none" stroke="transparent" stroke-width="12"
                      style="cursor: pointer;"/>
                <path t-att-d="arrow.d"
                      t-att-class="arrow.pathClass"
                      t-att-data-pred-id="arrow.id"
                      t-att-marker-end="'url(#gantt_arrowhead' + (arrow.markerClass ? '_' + arrow.markerClass : '') + ')'"/>
                <t t-if="arrow.lagLabel">
                    <text t-att-x="arrow.lagX" t-att-y="arrow.lagY"
                          class="o_gantt_arrow_lag_label"
                          t-att-text-anchor="arrow.lagAnchor or 'start'">
                        <t t-esc="arrow.lagLabel"/>
                    </text>
                </t>
            </t>
        </svg>
    </t>

    <!-- Alternate views overlay (resource / network / calendar) -->
    <t t-name="dobtor_project.GanttAltView">
        <div class="o_gantt_altview">
            <div class="o_gantt_altview_bar">
                <span class="o_gantt_altview_tabs">
                    <span class="o_gantt_altview_tab" t-on-click="() => this.close()" title="甘特圖">
                        <i class="fa fa-tasks"/> 甘特圖
                    </span>
                    <span t-att-class="'o_gantt_altview_tab' + (props.mode === 'resource' ? ' active' : '')"
                          t-on-click="() => this.setMode('resource')" title="資源">
                        <i class="fa fa-users"/> 資源
                    </span>
                    <span t-att-class="'o_gantt_altview_tab' + (props.mode === 'network' ? ' active' : '')"
                          t-on-click="() => this.setMode('network')" title="網路圖">
                        <i class="fa fa-sitemap"/> 網路圖
                    </span>
                    <span t-att-class="'o_gantt_altview_tab' + (props.mode === 'calendar' ? ' active' : '')"
                          t-on-click="() => this.setMode('calendar')" title="日曆">
                        <i class="fa fa-calendar"/> 日曆
                    </span>
                </span>
                <span class="o_gantt_altview_close" t-on-click="() => this.close()" title="關閉">
                    <i class="fa fa-times"/>
                </span>
            </div>

            <!-- Resource view -->
            <t t-if="props.mode === 'resource'">
                <div class="o_gantt_altview_body o_gantt_resview">
                    <div t-if="!resourceLanes.length" class="o_gantt_altview_empty">無資源指派資料</div>
                    <t t-foreach="resourceLanes" t-as="lane" t-key="lane.id">
                        <div class="o_gantt_resview_lane">
                            <div class="o_gantt_resview_name" t-esc="lane.name"/>
                            <div class="o_gantt_resview_track" t-att-style="'width:' + timelineWidth + 'px;'">
                                <t t-foreach="lane.overloads" t-as="ov" t-key="ov_index">
                                    <div class="o_gantt_resview_overload"
                                         t-att-style="'left:' + ov.left + 'px;width:' + ov.width + 'px;'"
                                         title="資源過載（時段重疊）"/>
                                </t>
                                <t t-foreach="lane.tasks" t-as="task" t-key="task.id">
                                    <div class="o_gantt_resview_bar" t-att-style="_barStyle(task)"
                                         t-att-title="recName(task)">
                                        <span t-esc="recName(task)"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </t>
                </div>
            </t>

            <!-- Network / PERT view -->
            <t t-if="props.mode === 'network'">
                <div class="o_gantt_altview_body o_gantt_netview">
                    <t t-set="net" t-value="networkLayout"/>
                    <svg class="o_gantt_net_svg" t-att-width="net.width" t-att-height="net.height">
                        <defs>
                            <marker id="gantt_net_arrow" markerWidth="8" markerHeight="8"
                                    refX="7" refY="3" orient="auto">
                                <path d="M0,0 L7,3 L0,6 Z" fill="#8e8e93"/>
                            </marker>
                        </defs>
                        <t t-foreach="net.links" t-as="lk" t-key="lk_index">
                            <line t-att-x1="lk.x1" t-att-y1="lk.y1" t-att-x2="lk.x2" t-att-y2="lk.y2"
                                  stroke="#8e8e93" stroke-width="1.5" marker-end="url(#gantt_net_arrow)"/>
                        </t>
                        <t t-foreach="net.nodes" t-as="nd" t-key="nd.id">
                            <g>
                                <rect t-att-x="nd.x" t-att-y="nd.y" t-att-width="nd.w" t-att-height="nd.h"
                                      rx="8" t-att-class="'o_gantt_net_node' + (nd.critical ? ' critical' : '')"/>
                                <text t-att-x="nd.x + 10" t-att-y="nd.y + 22"
                                      class="o_gantt_net_label" t-esc="nd.name"/>
                                <text t-att-x="nd.x + 10" t-att-y="nd.y + 42"
                                      class="o_gantt_net_sub" t-esc="nd.progress + '%'"/>
                            </g>
                        </t>
                    </svg>
                    <div t-if="!net.nodes.length" class="o_gantt_altview_empty">無任務資料</div>
                </div>
            </t>

            <!-- Calendar view -->
            <t t-if="props.mode === 'calendar'">
                <div class="o_gantt_altview_body o_gantt_calview">
                    <div class="o_gantt_cal_title" t-esc="calendarTitle"/>
                    <!-- Planning mode: relative T+N grid (native calendar can't show this) -->
                    <t t-if="calendarIsPlanning">
                        <div class="o_gantt_cal_grid o_gantt_cal_grid_planning">
                            <t t-foreach="planningCalendarRows" t-as="week" t-key="week.key">
                                <t t-foreach="week.days" t-as="day" t-key="day.key">
                                    <div class="o_gantt_cal_cell">
                                        <div class="o_gantt_cal_daynum o_gantt_cal_planlabel" t-esc="day.label"/>
                                        <t t-foreach="day.tasks" t-as="task" t-key="task.id">
                                            <div class="o_gantt_cal_task" t-att-title="recName(task)"
                                                 t-esc="recName(task)"/>
                                        </t>
                                        <div t-if="day.more" class="o_gantt_cal_more" t-esc="'+' + day.more"/>
                                    </div>
                                </t>
                            </t>
                        </div>
                    </t>
                    <!-- Real dates: month grid -->
                    <t t-else="">
                        <div class="o_gantt_cal_grid">
                            <t t-foreach="weekdayLabels" t-as="wd" t-key="wd_index">
                                <div class="o_gantt_cal_weekday" t-esc="wd"/>
                            </t>
                            <t t-foreach="calendarWeeks" t-as="week" t-key="week.key">
                                <t t-foreach="week.days" t-as="day" t-key="day.key">
                                    <div t-att-class="'o_gantt_cal_cell' + (day.inMonth ? '' : ' o_gantt_cal_out')">
                                        <div class="o_gantt_cal_daynum" t-esc="day.label"/>
                                        <t t-foreach="day.tasks" t-as="task" t-key="task.id">
                                            <div class="o_gantt_cal_task" t-att-title="recName(task)"
                                                 t-esc="recName(task)"/>
                                        </t>
                                        <div t-if="day.more" class="o_gantt_cal_more" t-esc="'+' + day.more"/>
                                    </div>
                                </t>
                            </t>
                        </div>
                    </t>
                </div>
            </t>
        </div>
    </t>

</templates>
